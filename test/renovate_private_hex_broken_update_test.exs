defmodule RenovatePrivateHexBrokenUpdateTest do
  use ExUnit.Case
  doctest RenovatePrivateHexBrokenUpdate

  test "greets the world" do
    assert RenovatePrivateHexBrokenUpdate.hello() == :world
  end
end
